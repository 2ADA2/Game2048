const UserList = require('../models/models');
const ApiError = require('../error/ApiError');

class ListController {
    async create(req, res) {
    }

    async getList(req, res) {

    }
}

module.exports = new ListController()